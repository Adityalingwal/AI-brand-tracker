{
    "title": "AI Brand Tracker",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "category": {
            "title": "Industry Category",
            "type": "string",
            "description": "The industry or niche to analyze (e.g., 'CRM software', 'project management tools', 'email marketing platforms')",
            "editor": "textfield",
            "prefill": "CRM software"
        },
        "yourBrand": {
            "title": "Your Brand",
            "type": "string",
            "description": "Your brand name to track visibility for",
            "editor": "textfield",
            "prefill": "Salesforce"
        },
        "competitors": {
            "title": "Competitor Brands (Optional)",
            "type": "array",
            "description": "Competitor brands to compare against. Leave empty to track only your brand.",
            "editor": "stringList",
            "prefill": [],
            "maxItems": 10
        },
        "platforms": {
            "title": "AI Platforms",
            "type": "array",
            "description": "Select which AI platforms to query (no API keys needed!)",
            "editor": "select",
            "items": {
                "type": "string",
                "enum": ["chatgpt", "gemini", "perplexity"],
                "enumTitles": ["ChatGPT (OpenAI)", "Gemini (Google)", "Perplexity AI"]
            },
            "prefill": ["chatgpt"],
            "minItems": 1
        },
        "promptCount": {
            "title": "Number of Prompts",
            "type": "integer",
            "description": "How many template prompts to use (1-5). Ignored if you provide custom prompts.",
            "default": 1,
            "minimum": 1,
            "maximum": 5,
            "prefill": 1
        },
        "customPrompts": {
            "title": "Custom Prompts (Optional)",
            "type": "array",
            "description": "Your own prompts to use instead of templates. When provided, template prompts are skipped. Example: 'What is the best CRM for startups?'",
            "editor": "stringList",
            "maxItems": 5
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings - recommended to avoid rate limits on AI platforms",
            "editor": "proxy",
            "prefill": {
                "useApifyProxy": true
            }
        }
    },
    "required": ["category", "yourBrand", "platforms"]
}
